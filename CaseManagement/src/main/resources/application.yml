server:
  port: 8082

spring:
  application:
    name: case-service
  datasource:
    url: jdbc:h2:mem:case-db
    driverClassName: org.h2.Driver
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true